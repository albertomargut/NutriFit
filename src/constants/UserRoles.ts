import { Role } from "../models/Role";

// -----------------------------------------------------------------------------


export const UserRoles = {
   ADMIN: { id: 1, name: "admin" } as Role,
   CLIENT: { id: 2, name: "client" } as Role,
   NUTRITIONIST: { id: 3, name: "nutritionist" } as Role,
};
